# Beispiel-ENV für das Backend (kopieren nach .env)
# Lokale PostgreSQL (Docker Compose nutzt Port 5433)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/budget-tracker?schema=public&sslmode=disable
# Optional: direkter URL (z. B. für Neon)
DIRECT_DATABASE_URL=
# API Port
PORT_API=3001
# CORS Origins (kommagetrennt)
CORS_ORIGIN=http://localhost:4201
