{
  "name": "@budget-tracker/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'Worker builds via wrangler, no separate build needed'",
    "deploy": "wrangler deploy",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@prisma/adapter-neon": "6.18.0",
    "@prisma/client": "6.18.0",
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "prisma": "6.18.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.45.0"
  },
  "prisma": {
    "schema": "../backend/prisma/schema.prisma"
  }
}
