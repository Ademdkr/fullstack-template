# Frontend example environment
# Kopiere nach apps/frontend/.env oder nutze tools/setup.mjs um Platzhalter zu ersetzen

# API base url used by the frontend (relative path for local dev + proxy)
API_BASE_URL=/api

# Port for local dev server (used by tools/setup.mjs)
PORT_WEB=4201
