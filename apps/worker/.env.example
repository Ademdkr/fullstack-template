# Worker (Cloudflare / Serverless) example env
# For local dev, put secrets in apps/worker/.dev.vars or use wrangler secret
DATABASE_URL=""

# If using Neon/Serverless DB, fill DIRECT and other adapter-specific values as needed
